Gates API
=========

.. automodule:: abirqu.gates
   :members:
   :undoc-members:
   :show-inheritance:

Single-Qubit Gates
------------------

- ``X`` — Pauli-X (NOT) gate
- ``Y`` — Pauli-Y gate
- ``Z`` — Pauli-Z gate
- ``H`` — Hadamard gate
- ``S`` — S (phase) gate
- ``S_dag`` — S-dagger gate
- ``T`` — T gate
- ``T_dag`` — T-dagger gate

Parameterized Gates
-------------------

- ``rx(theta)`` — Rotation around X-axis
- ``ry(theta)`` — Rotation around Y-axis
- ``rz(theta)`` — Rotation around Z-axis

Two-Qubit Gates
---------------

- ``CNOT`` — Controlled-NOT gate
- ``CZ`` — Controlled-Z gate
- ``SWAP`` — Swap gate
